'use strict';

describe('creditCardageApp.homeView module', function() {

  beforeEach(module('creditCardageApp.homeView'));

  describe('homeView controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var homeViewCtrl = $controller('homeViewCtrl');
      expect(homeViewCtrl).toBeDefined();
    }));

  });
});
